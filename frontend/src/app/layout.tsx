import type { Metadata } from "next";
import { Inter, JetBrains_Mono } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

const jetbrainsMono = JetBrains_Mono({
  subsets: ["latin"],
  variable: "--font-mono",
  display: "swap",
});

export const metadata: Metadata = {
  title: {
    default: "Collaborative Database Notebook",
    template: "%s | Collaborative DB Notebook",
  },
  description:
    "A web-based, Jupyter-style notebook for querying PostgreSQL, MySQL, MongoDB, and SQLite — with inline results, schema explorer, and in-app tutorials.",
  keywords: ["database", "notebook", "SQL", "MongoDB", "query", "collaborative"],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={`${inter.variable} ${jetbrainsMono.variable}`}>
      <body>{children}</body>
    </html>
  );
}
